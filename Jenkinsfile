pipeline {
    agent { label 'master' }
    parameters {
        string(name: '', defaultValue: '', description: '')
    }
    stages {
        stage('') {
            steps {
            }
        }
    }
}
